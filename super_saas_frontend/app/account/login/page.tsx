"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { requestCustomerCode } from "@/lib/customerAuth";
export default function CustomerLoginPage(){const [phone,setPhone]=useState("");const [terms,setTerms]=useState(false);const [error,setError]=useState("");const [loading,setLoading]=useState(false);const router=useRouter();return <main className="mx-auto flex min-h-screen max-w-md flex-col justify-center p-6"><h1 className="text-2xl font-black">Entrar na minha conta</h1><p className="mt-2 text-sm text-slate-600">Use somente seu telefone. Enviaremos um código para confirmar que o número é seu.</p><label className="mt-6 text-sm font-semibold">Telefone</label><input className="mt-2 rounded-xl border p-3" inputMode="tel" value={phone} onChange={e=>setPhone(e.target.value)} placeholder="(16) 99999-9999"/><label className="mt-4 flex gap-2 text-xs text-slate-600"><input type="checkbox" checked={terms} onChange={e=>setTerms(e.target.checked)}/>Aceito os termos e a política de privacidade e entendo o uso do telefone para autenticação.</label>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<button className="mt-5 rounded-xl bg-slate-950 p-3 font-bold text-white disabled:opacity-50" disabled={loading||!terms} onClick={async()=>{setLoading(true);setError("");try{await requestCustomerCode(phone,terms);sessionStorage.setItem("customer-auth-phone",phone);router.push("/account/verify")}catch(e:any){setError(e.message)}finally{setLoading(false)}}}>{loading?"Enviando...":"Receber código"}</button></main>}
